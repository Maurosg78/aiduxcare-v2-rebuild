/**
 * Mock para transcripción vacía (caso límite)
 * Para probar el comportamiento del sistema cuando no hay transcripción disponible
 */
import { TranscriptionSegment } from './transcript_with_errors';

export const MOCK_EMPTY_TRANSCRIPT: TranscriptionSegment[] = []; 